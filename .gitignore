# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI/LLM development context files
.aicontext
.claude/

# Session documents — the working notes a Claude Code session writes and reads.
# They are how a session hands off to the next one, not part of the app, and
# they date fast; the contracts they describe live in the tracked README and in
# GradeBridge2026/CLAUDE.md. Keep them local.
#
# This block existed, with this reasoning, while nineteen process documents were
# tracked anyway — nine of them REPORT_* under tests/captures/, which the list
# did not name. So: no leading slash on any pattern, because these arrive in
# subdirectories as often as at the root, and `tests/no-process-records.mjs`
# fails the suite if one is ever tracked. An ignore file is not a guard; it does
# nothing about a file somebody adds with `-f`, and that is how these got in.
HANDOFF_*.md
COMPLETION_*.md
WORKORDER_*.md
WORK_ORDERS_*.md
CORRECTION_*.md
REPORT_*.md
DIRECTIVE_*.md
DECISION_*.md
SESSION_*.md
MILESTONE_*.md
NOTE_*.md
PLAN_*.md
SCHEDULE_*.md
RESUME_*.md
HANDOVER_*.md
BRIEF_*.md
START_HERE*.md
*_Memo_*.md
*_Test_Battery_*.md

# A session note's screenshots are part of the note, and go the same way. The
# guard in `tests/no-process-records.mjs` reads `.md` basenames only, so an
# image attached to one of these is caught here or it is caught nowhere.
COMPLETION_*.png
WORKORDER_*.png
HANDOFF_*.png

# Generated capture set for the registration suite (npm run captures rebuilds it).
# Real photographs in tests/captures/real/ ARE tracked — they are the evidence.
tests/captures/synthetic/

# Photographs of other people's work. This repository is public and these images
# are not ours to publish: the `students/` captures are real submitted coursework
# and the `android/` set was contributed by a colleague for testing. They are the
# best evidence the capture pipeline has — LABELS.csv specifies all 41 rows —
# so the suite names the ones it cannot see rather than counting an absent
# photograph as agreement.
#
# The photographs that ARE tracked, in real/ and stale/, are the maintainer's
# own blank template with his own handwriting on it. See tests/captures/README.md.
tests/captures/students/
tests/captures/android/
tests/captures/generic_page/
# ...and anything a probe renders FROM them.
#
# IGNORE EVERYTHING A PROBE RENDERS, AND NAME THE EXCEPTION INSTEAD.
#
# This was a list of per-set prefixes until 2026-09-03. Two things were wrong
# with that. Renaming the capture sets that day silently stopped ignoring the
# old names, so the protection went out with the name — and the staging folders
# outside this repository still use the old names, which makes the sequence that
# hurts an ordinary one: copy those captures back in, run a probe, `git add -A`,
# and renders of real student coursework are trackable where a moment ago they
# were ignored. A prefix list also has to be edited at every rename, and the one
# rename it was not edited at is the one that mattered.
#
# So the default is now to ignore the whole folder and to name what is tracked.
# That covers the old prefixes, the current ones, and any prefix nobody has
# thought of yet; it cannot rot at the next rename; and it spells no personal
# name, which a prefix list necessarily did. Ignore rules do not untrack
# anything, so the IMG_* renders that are already committed stay committed; the
# exception below states that rather than leaving it to be inferred.
#
# To track a new probe render deliberately: `git add -f`.
tests/captures/_probe/*
!tests/captures/_probe/IMG_*.png
